import { PostCardSkeleton } from "../PostCard";

/**
 * SkeletonCard
 * Simple wrapper around PostCardSkeleton so it can be reused from `components/shared`.
 */
export default function SkeletonCard() {
  return <PostCardSkeleton />;
}
